import { Routes } from '@angular/router';
import { AppHomeComponent } from './Home/app.HomeComponent';

export const routes: Routes = [
    {
        path:"",
        component:AppHomeComponent
    }
];
